apiVersion: kappctrl.k14s.io/v1alpha1
kind: App
metadata:
  name: access-monitor-rabbitmq
  namespace: app-install
  annotations:
    kapp.k14s.io/change-group: "{name}"
    kapp.k14s.io/change-rule.create-order.1: "upsert after upserting access-monitor-ns"
    kapp.k14s.io/change-rule.delete-order.1: "delete before deleting access-monitor-ns"
    kapp.k14s.io/change-rule.create-order.2: "upsert after upserting rabbitmq-operator"
    kapp.k14s.io/change-rule.delete-order.2: "delete before deleting rabbitmq-operator"
spec:
  serviceAccountName: kapp
  fetch:
  - git:
      url: https://github.com/making/k8s-gitops.git
      ref: origin/main
      subPath: apricot/app/access-monitor-rabbitmq
  syncPeriod: 600h
  template:
  - sops:
      age:
        privateKeysSecretRef:
          name: age-key
  - ytt:
      paths:
      - config
  - kbld:
      paths:
      - '-'
  deploy:
  - kapp:
      rawOptions:
      - --wait-timeout=5m
      - --diff-changes=true
      - --diff-mask=false
      delete:
        #! Force delete PVCs, since StatefulSet does not delete them
        rawOptions:
        - --apply-ignored=true