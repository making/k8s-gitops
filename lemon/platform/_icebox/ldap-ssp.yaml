apiVersion: kappctrl.k14s.io/v1alpha1
kind: App
metadata:
  name: ldap-ssp
  namespace: app-install
  annotations:
    kapp.k14s.io/change-group: "{name}"
    kapp.k14s.io/change-rule.create-order.1: "upsert after upserting openldap"
    kapp.k14s.io/change-rule.delete-order.1: "delete before deleting openldap"
spec:
  serviceAccountName: kapp
  fetch:
  - helmChart:
      name: application
      version: "6.14.0"
      repository:
        url: https://stakater.github.io/stakater-charts
  - git:
      url: https://github.com/making/k8s-gitops.git
      ref: origin/main
      subPath: lemon/platform/ldap-ssp
  - git:
      url: https://github.com/categolj/ldap-ssp.git
      ref: origin/images
  syncPeriod: 240h
  template:
  - sops:
      age:
        privateKeysSecretRef:
          name: age-key
  - helmTemplate:
      name: ldap-ssp
      namespace: ldap-ssp
      path: "0"
      valuesFrom:
      - path: 1/helm-values.yaml
      - path: 2/native_aarch64.yaml
  - ytt:
      paths:
      - "-"
      - 1/config
      valuesFrom:
      - path: 1/credentials.yaml
  - kbld:
      paths:
      - '-'
  deploy:
  - kapp:
      rawOptions:
      - --wait-timeout=5m
      - --diff-changes=true
      - --diff-mask=true
      delete:
        #! Force delete PVCs, since StatefulSet does not delete them
        rawOptions:
        - --apply-ignored=true