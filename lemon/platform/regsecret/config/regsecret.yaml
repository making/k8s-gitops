#@ load("@ytt:data", "data")
apiVersion: v1
kind: Secret
metadata:
  name: regsecret
  namespace: app-install
type: kubernetes.io/dockerconfigjson
stringData:
  #@yaml/text-templated-strings
  .dockerconfigjson: |
    {
      "auths": {
        "(@= data.values.registry_url @)": {
          "username": "(@= data.values.registry_username @)",
          "password": "(@= data.values.registry_password @)"
        }
      }
    }